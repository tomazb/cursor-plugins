# Changelog

## 1.0.0

- Initial Rubber Duck plugin release.
- Skill: `rubber-duck` — consult a constructive critic on a contrasting model.
- Agent: `rubber-duck` — read-only critic (Blocking / Non-blocking / Suggestions).
- Rule: `consult-rubber-duck` — when to seek a second opinion automatically.
- Command: `/rubber-duck` — manual critique with an optional focus question.
